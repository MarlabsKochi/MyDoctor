<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "My Doctor" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "My Doctor" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%#= render 'layouts/navigation' %>
      <% if action_name.to_s != "time_selector" %>
        <%= render 'layouts/bootstrap' %>
      <% end %>
      <% if action_name.to_s != "new" && action_name.to_s != "show" && !current_doctor.present? && !current_patient.present? %>
        <%#= render 'layouts/sliders' %>
        <%= render 'layouts/about_us' %>
      <% end %>
    </header>
    <main role="main">
       <%= render 'layouts/messages' %>
       <div style="padding-top:100px;">
         <%= yield %>
       </div>
    </main>
  </body>
</html>
